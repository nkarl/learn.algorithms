#include "../utils.hpp"

/**
 * MAIN ENTRY.
 */
int main(int argc, char *argv[]) {
  vector<int> nums = {1, 2, 3, 4, 5, 6};
  myPrint(nums); // before
  /*
   * Add solution function(s) here.
   */
  myPrint(nums); // after
  return 0;
}
